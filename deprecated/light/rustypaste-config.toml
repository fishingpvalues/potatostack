[server]
address = "0.0.0.0:8000"
workers = 1
max_content_length = "50MB"
upload_path = "/data/uploads"
timeout = "15s"

[landing_page]
text = """
# RustyPaste - PotatoStack Light

Simple, fast file/text sharing server optimized for Le Potato.

## Usage

### Upload a file:
```bash
curl -F "file=@example.txt" http://192.168.178.40:8787
```

### Upload from clipboard:
```bash
curl -F "file=@-" http://192.168.178.40:8787 <<< "text content"
```

### Set custom URL:
```bash
curl -F "file=@example.txt" -F "url=custom-name" http://192.168.178.40:8787
```

### Upload with expiry (1 hour):
```bash
curl -F "file=@example.txt" -F "expire=1h" http://192.168.178.40:8787
```

### Upload with expiry (1 day):
```bash
curl -F "file=@example.txt" -F "expire=1d" http://192.168.178.40:8787
```

Supported expiry: s (seconds), m (minutes), h (hours), d (days)
"""

[landing_page_footer]
text = "PotatoStack Light • 2GB RAM Optimized"

[paste]
random_url.enabled = true
random_url.type = "alphanumeric"
random_url.length = 6
duplicate_files = true
default_extension = "txt"
delete_expired_files.enabled = true
delete_expired_files.interval = "4h"
