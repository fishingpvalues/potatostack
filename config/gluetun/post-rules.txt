# Killswitch hardening is applied via gluetun-monitor.sh using docker exec,
# because gluetun's post-rules.txt parser does not support -m conntrack/-m state.
# See scripts/monitor/gluetun-monitor.sh: apply_killswitch_hardening()
